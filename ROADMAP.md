# 산초 (Sancho) — 프로젝트 로드맵 & 원격 작업 가이드

산초는 **내 컴퓨터에서 구동되는 로컬 AI 개인 비서(PAIS)와 화이트 톤 전사 업무 포털**을 결합한 독립형 업무 플랫폼입니다.

외부 라이브러리 설치가 필요 없는 순수 Node.js(Zero-dependency)로 동작하며, 특정 회사 전용 도구를 배제하여 **누구나 자신의 PC나 팀에서 즉시 내려받아 사용할 수 있는 범용 플랫폼**으로 설계되었습니다.

---

## 📌 주요 설계 원칙

1. **설치 없는 경량 실행 (Zero-dependency)**
   - `npm install`이나 복잡한 빌드 과정 없이, Node.js 20+ 환경만 있으면 `git clone` 직후 즉시 구동됩니다.
2. **단일 진실 파일 저장소 (File-based DB)**
   - 외부 DBMS(MySQL, MongoDB 등) 없이 `data/db/<컬렉션>.json` 파일로 모든 데이터를 관리합니다.
   - SSE(Server-Sent Events)를 통해 AI나 사용자가 파일을 수정하면 열려 있는 모든 화면이 실시간으로 갱신됩니다.
3. **Firestore 호환 클라이언트 (`public/m/sdb.js`)**
   - 웹 표준 Firestore 문법(`collection`, `doc`, `onSnapshot`, `updateDoc` 등)을 모방하여 모듈 화면들이 독립적이고 표준적으로 동작합니다.
4. **개인정보 및 보안 격리**
   - `data/` 디렉터리는 `.gitignore`에 등록되어 사내 정보, 대화 기록, 기밀 데이터가 GitHub에 커밋되지 않습니다.

---

## 🧭 개발 현황 및 모듈 안내 (13개 화면 완비)

| 모듈 ID | 화면명 | 주요 기능 및 차별점 |
|---|---|---|
| `dashboard` | **대시보드 (산초 채팅)** | 오늘 KPI 4장(일정·결재·메일·프로젝트), 빠른 지시 버튼, 스트리밍 대화, 파일 첨부, 음성 입출력, 🧠 뇌 그래프 |
| `calendar` | **일정 관리** | 월간/주간 캘린더, 개인 일정, 마감일, 회의 예약, 산초 스케줄 통합 뷰 |
| `projects` | **프로젝트 & WBS** | 프로젝트 카드, 연간 통합 간트, 업무 칸반, 계층형 WBS 공정표(EVMS, SPI, S-곡선, 지연 진단) |
| `mail` | **메일 정리** | 수신 메일 AI 자동 분류(업무/개인/광고), 긴급도 판정, 핵심 요약, 답장 초안 생성 |
| `messenger` | **사내 메신저** | 공지/부서/프로젝트/DM 채널, 파일 공유, 산초 AI 어시스턴트 참여 |
| `meeting` | **회의실 & 회의록** | 주간 회의실 예약, 음성 녹취 텍스트 변환, AI 자동 회의록 요약 및 액션 아이템 도출 |
| `okr` | **목표 관리 (OKR)** | 전사/부서/개인 목표 수립, 하위 마일스톤 가중 진행률 자동 집계 |
| `manday` | **공수 관리 (Manday)** | 자연어 텍스트 한 줄 복사-붙여넣기로 프로젝트별·일자별 공수(M/D) 자동 분석 및 집계 |
| `org` | **조직 / 권한** | 부서 트리 관리, 구성원 명부, 권한 등급, 반응형 조직도 시각화 |
| `admin` | **플랫폼 관리** | AI 두뇌 상태 점검, 모델/노력값 설정, 스케줄 실행 로그, 시스템 에러 로그, 데이터 검사 |
| `approval` | **전자 결재** | 기안서 작성, 다단계 결재선(검토/승인/전결/반려), 서식별 인쇄 양식 지원 |
| `roadmap` | **로드맵 & 진척** | 단계별 마일스톤 관리, 항목 상태(대기/진행/완료/보류) 토글, 원형 진척률 그래프 |
| `knowledge` | **지식노트** | 회사 업무 데이터(프로젝트·회의·결재·인물·위키)를 연결한 3D 지식 그래프 및 마크다운 위키 |
| `help` | **도움말 (매뉴얼)** | 3단계 시작 가이드, 화면별 산초 지시 예시문, 단축키, 자주 묻는 질문(FAQ) |

> **부서 도구함 관련 결정 사항**:  
> 특정 사내 전용 기술 도구(원자력/ASME/교반기 계산 등)는 플랫폼 범용성과 보안을 위해 메인 플랫폼에서 분리되었으며, 13개 핵심 협업 모듈에 집중하도록 정제되었습니다.

---

## 💻 다른 PC에서 작업 이어하기 (원격 개발 가이드)

다른 컴퓨터(집 PC, 사무실 PC, 노트북 등)에서 산초를 클론하여 개발을 이어가는 방법입니다.

### 1. 사전 준비
* **Git**: 설치 필수
* **Node.js**: v20.0.0 이상 권장 (`node -v`로 확인)
* **Claude Code**: 터미널에서 AI 두뇌를 연동하려면 Claude CLI 설치 및 로그인 필요 (`claude /login`)

### 2. 저장소 클론 및 실행
```bash
# 1. 저장소 복제
git clone https://github.com/cwkim83/sancho_project.git
cd sancho_project

# 2. 실행 (별도의 npm install 불필요!)
# Windows의 경우 sancho.bat 더블 클릭 또는 CLI 실행:
node server.js
```

브라우저에서 **`http://localhost:8790`** 접속 시 즉시 산초 플랫폼이 구동됩니다.

### 3. 검증 및 테스트 실행
새로운 화면이나 코드를 수정한 뒤 무결성을 검증합니다:

```bash
# 1. 전체 단위/통합 자가 테스트 (75개 검사 항목)
node selftest.js

# 2. 브라우저 헤드리스 렌더링 및 콘솔 무오류 검증 (Chrome 필요)
node probe.js http://127.0.0.1:8790 3500
```

---

## 🚀 향후 로드맵 (Milestones)

### 🚩 Phase 1: 로컬 단일 실행 & 핵심 업무 플랫폼 구축 (완료)
- [x] Node.js 무의존성 경량 백엔드 (`server.js`) 구축
- [x] 파일 기반 실시간 데이터베이스 (`/api/db`, SSE 알림)
- [x] 세종 플랫폼 v2 화이트 톤 디자인 시스템 이식 (`public/index.html`, `platform.css`)
- [x] 13개 업무 모듈 프론트엔드 제작 완료
- [x] 자가진단 검증 도구 (`selftest.js`, `probe.js`) 구축

### 🚩 Phase 2: 타 PC 협업 환경 & 배포 패키징 (진행 중)
- [x] 사내 고유 도구함 제거 및 범용 플랫폼 코드베이스 정제
- [x] 원격 협업 로드맵 및 매뉴얼 작성 (`ROADMAP.md`)
- [ ] 첫 실행 시 샘플 데모 데이터 원클릭 초기화 (`/api/seed`)
- [ ] Windows 백그라운드 서비스 등록 및 자동 업데이트 스크립트 고도화
- [ ] Mac / Linux 환경 구동 셸 스크립트 (`sancho.sh`) 제공

### 🚩 Phase 3: 외부 연동 & 알림 채널 확장 (예정)
- [ ] 백그라운드 일정/결재 알림 외부 발송 (Telegram 봇, 카카오톡 나에게 보내기, SMTP 이메일)
- [ ] Cloudflare Tunnel / ngrok 안전한 외부 접속 가이드 및 토큰 2차 인증
- [ ] 모바일 브라우저 전용 반응형 레이아웃 및 제스처 최적화

### 🚩 Phase 4: 멀티 LLM & 지능화 고도화 (예정)
- [ ] Claude Code 외에 Google Gemini, 로컬 Ollama, OpenAI 호환 API 키 직접 연동 지원
- [ ] `data/wiki/` 및 로컬 문서 파일 RAG(검색 증강 생성) 기반 지식 Q&A 기능 강화
- [ ] 음성 제어(STT/TTS) 정확도 개선 및 오프라인 모드 지원
