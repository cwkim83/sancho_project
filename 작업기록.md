# 산초 작업기록

## 2026-09-12 (금) 새벽 — 초판 착수
- 00:50 부장님 지시: "파이스처럼 이런 걸 산초로 만들어줘. 화면은 안 예뻐도 됨. 한도 차기 전에 멈추고 재개 예약, 풀리면 이어서. 아침에 볼 수 있게."
- 00:55 재개 안전망 설치: 이 세션 크론(매시 17분, NEXT.md 보고 이어감) + 데스크톱 예약 작업(05:35 1회, 세션이 죽었을 때 대비). 컴퓨터 깨어있기 요청.
- 00:40 설계 확정: 두뇌 = `claude -p --output-format stream-json --verbose`(구독 로그인, 관문·API 키 없음). 도구는 안 만든다 — memory.md / schedule.json 파일 규칙을 시스템 프롬프트에 적고 Claude Code 의 파일 도구가 그대로 도구가 된다. 산초 서버는 화면·시계(예약)·기억·알림만.
- 00:42 첫 판 커밋(e3987f1): server.js 약 190줄, index.html 약 100줄, README, sancho.bat.
- 00:45 `claude -p` 연기 시험: 플래그·stdin·stream-json 전부 정상. **그러나 이 PC 의 claude CLI 는 로그인이 안 돼 있음**("Not logged in · Please run /login", `claude auth status` loggedIn false). 데스크톱 앱의 Claude Code 로그인과 터미널 `claude` 로그인은 별개. → 진짜 답 시험은 부장님이 터미널에서 `claude` → `/login` 한 번 하신 뒤에 가능. 오류 문구는 산초 화면에 한국어 안내로 바꿔 보이게 함.
- 00:50 한도 안 쓰는 자가시험 selftest.js 작성(가짜 두뇌로 채팅 SSE·예약 tick·■중지 검사). SANCHO_DATA / SANCHO_CLAUDE / SANCHO_PORT 환경변수로 시험 격리. 한 번에 통과.
- 00:55 브라우저 실물 확인(127.0.0.1:8790): 화면 렌더 정상, 전송 정상, "로그인 안 됨" 한국어 안내 말풍선 정상, 콘솔 오류 0. 고친 것: 한글 조합 중 Enter 무시, 합성 오류 문구 중복 말풍선 제거, 대화 id 는 성공한 답만 저장, 이미 켜져 있을 때 안내 후 종료.
- 01:00 커밋 5개. 재개 안전망(매시 17분 크론, 05:35 예약 작업)은 필요 없어져 삭제.

## 아침 보고 (부장님께)

### 된 것
- 산초 초판이 `C:\Users\USER\Sancho` 에 있습니다. 커밋 5개, 코드 461줄(server.js 242 · index.html 122 · selftest.js 97), 의존 패키지 0.
- 구조: 브라우저 챗창+대시보드 → server.js(화면·예약·기억·알림) → `claude -p --output-format stream-json`(본인 구독 로그인) → data/ 텍스트 파일. **관문도 API 키도 없습니다.**
- 기능: 채팅(스트림, 도구 사용 표시, ■ 중지, 대화 이어가기·새 대화) · 기억(memory.md) · 예약(schedule.json, 놓친 회차는 켜지면 실행, '지금' 버튼) · 알림(journal/) · 권한 토글(명령 실행 / 홈 폴더 접근) · 모델 선택(sonnet/opus/haiku).
- 검증: `node selftest.js` 통과 — 가짜 두뇌로 채팅·예약 tick·■중지를 검사하며 구독 한도를 쓰지 않습니다. 브라우저 실물 확인도 마쳤습니다. `claude -p` 연기(플래그·stdin·stream-json)도 전부 동작 확인.

### 안 된 것 — 부장님 손이 필요합니다
- **진짜 답 시험을 못 했습니다.** 이 PC 의 터미널용 `claude.exe`(2.1.201) 가 **로그인이 안 돼 있습니다**(`claude auth status` → loggedIn false). 데스크톱 앱 로그인과는 별개입니다. 제가 대신 로그인할 수는 없습니다.

### 아침에 하실 것 (3분)
1. PowerShell 에서 `claude` → `/login` → 브라우저에서 로그인 (한 번만)
2. `C:\Users\USER\Sancho\sancho.bat` 더블클릭 → http://127.0.0.1:8790
3. 채팅에 "기억해: 주인은 골프를 좋아한다" → 오른쪽 '기억' 패널에 뜨는지
4. "매일 07:00 에 오늘 할 일 정리해서 알려줘" → '예약' 패널에 뜨면 '지금' 버튼 → '알림' 패널에 결과

### 진짜 두뇌 시험 결과 (2026-09-12 18:50~19:00, 부장님 `/login` 뒤)
- 부장님이 먼저 쓰신 것: "기억해 주인은 산초를 좋아한다" → memory.md 반영(9초), "너가 할 수 있는 일이 머야?" → 대화 이어가기 정상(12초). 설정을 opus · 명령 실행 · 홈 폴더 접근 허용으로 바꾸심.
- 예약: "매일 07:00에 오늘 할 일을 정리해서 알려줘" → Claude 가 schedule.json 에 `morning-todo` 등록(18.7초) → 07:00 이 이미 지난 시각이라 서버가 **놓친 회차로 바로 실행** → journal/2026-09-12.md 에 브리핑 → 알림 패널 표시. 한 바퀴 전부 정상.
- ■ 중지: 긴 작업을 첫 도구 사용 시점(5.7초)에 끊음 → 0.2초 뒤 종료, 서버 한가함 복귀. 통과.
- 고친 것: 예약 일지에 중간 혼잣말("I'll check…")까지 쌓여서 마지막 답만 남기도록 변경(3ab1385).

## 2026-09-12 (금) 저녁 — 자가 업그레이드
- GitHub 공개 저장소 cwkim83/sancho_project 생성·push (부장님 gh 로그인 뒤).
- 설계: 도구를 새로 만들지 않는다(Claude Code 의 Edit/Bash 가 도구). 만든 건 안전장치 셋 — ① 재시작 관문(`POST /api/restart` 가 `node --check` + `node selftest.js` 를 직접 돌려 실패하면 거부) ② 마지막 정상판 표시(정상 기동 + 작업 폴더 깨끗할 때 `git tag -f last-good`) ③ 실행기 루프(sancho.bat: 75 = 재시작, 그 외 비정상 종료 = last-good 복구 후 재시작, 3연속 실패면 멈춤). 여기에 업데이트 버튼(`git pull --ff-only` → 관문 → 재시작, 관문 실패 시 이전 판으로) 과 "자기 수정 허용" 토글(시스템 프롬프트에 코드 위치·절차 추가, 산초 폴더 --add-dir).
- 재시작은 진행 중인 일이 끝난 뒤 일어난다(재시작예약) — 채팅 중 Claude 가 재시작을 요청해도 답이 끊기지 않게.
- 시험: selftest 확장(화면 스크립트 문법, 재시작 관문·예약, 종료 75) 통과. 임시 클론에서 복구 실물 시험 — server.js 를 깨고 프로세스를 죽이니 sancho.bat 이 last-good 으로 되돌리고 다시 켬(bat.log "crashed with code 1. Rolling back"). 실서버에서 `/api/restart` → 관문 6초 → 종료 75 → 재시작 → 새 pid 로 응답, `/api/update` → 이미 최신.
- 배운 것 둘: 한글 파일명(산초시작.bat)과 **배치 파일 안 한글 주석**은 cmd 가 오독한다 → 실행기는 `sancho.bat`, 내용은 영문 전용.
- 자기 수정 실전 시험 1차: 시스템 프롬프트에 절차(고치기→자가시험→커밋→재시작 요청)를 적고 "절차대로 해줘" 라고 했는데 Claude 는 **고치고 끝냈다**(자가시험·커밋·재시작 없음, 17초). → 절차를 모델에게 맡기지 않고 **서버가 한다**로 바꿈: 답이 끝나면 산초 코드에 변경이 있으면 관문 → 커밋(`산초 자기 수정: <요청문>`) → 재시작, 관문 실패면 `git checkout -- .` 로 버리고 채팅에 이유 표시(b7992db).
- 자기 수정 실전 시험 2차(서버 마무리): Edit → 답 → 서버 관문 6초 → 커밋 → 종료 75 → sancho.bat 재시작 → 새 pid → last-good 이동. 전부 자동. 채팅 화면엔 "🔁 … 커밋됐어요. 지금 하는 일이 끝나면 재시작합니다" 말풍선.
- 시험 도구 함정: Git Bash 의 `curl -d '한글'` 은 본문이 깨져 전달돼 Claude 가 요청문을 못 읽고 문구를 지어냈다(명령하세요). 파일로 보내니(`--data-binary @file`) 요청 그대로 반영(말씀하세요). 브라우저 경로는 원래 정상. 산초 문제 아님.

## 2026-09-12 (금) 밤 — 화면을 Claude 앱처럼
- 부장님 요청: "대화창의 느낌과 기능, 모델 선정 버튼 위치, 취소 버튼, UI 전부 Claude 처럼". Claude 데스크톱 앱 스크린샷 기준으로 다시 그림.
- 왼쪽 사이드바: ✳ 산초 · ＋ 새 대화 · 대화 목록(제목=첫 말 40자, 상대 시각) · 예약(▶ 지금 / ✕ 삭제는 올렸을 때) · 알림(접이식) · 기억(접이식) · 아래 상태 + ⚙(업데이트·재시작).
- 가운데: 제목줄(대화 제목 · 모델 칩 · "✳ 실행 중인 작업 1개"), 답은 말풍선 없이 마크다운 렌더링(marked 12, 표·굵게·코드·인라인 코드 붉은 배경), 사용자 말은 연한 둥근 상자, 도구 사용은 ⏺ 줄, 편집한 파일은 "파일 N개 편집됨" 카드, 자기 수정 결과는 🔁 카드.
- 입력창: 둥근 카드, 자동 높이, 왼쪽 "권한 ▾"(명령·홈 폴더·자기 수정 토글), 오른쪽 "모델 ▾"(Sonnet/Opus/Haiku) + 원형 ↑ 전송 → 실행 중엔 ■ 중지. Esc 로도 중지.
- 서버: 대화 목록(state.sessions) · 기록에 sid 붙여 대화별로 불러오기 · `POST /api/session` 으로 이전 대화 이어가기(--resume). 새 대화는 빈 화면(옛 기록 안 섞임).
- 실전 확인(진짜 Claude, opus): "산초가 할 수 있는 일을 표로 짧게 정리해줘" → 표가 표로 렌더링, 제목줄에 대화 제목·모델 칩, 왼쪽 대화 목록에 항목 생성, 10.9초. 고친 것: 메뉴 hidden 시 숨김(display:flex 가 hidden 을 이김), 첫 말 보내면 빈 화면 안내 제거.
- 아쉬운 점(다음 판): 대화 삭제 · 대화 제목 편집 · 스트리밍 글자 단위 표시(--include-partial-messages) · 다크 모드.

## 2026-09-13 (토) 새벽 — Fable · 노력 · 첨부 · Hermes 기능(스킬·위키·배달)
- 부장님 요청: Fable 모델 추가, Claude 앱의 "노력" 슬라이더처럼 선택, 어제 파이스에 넣은 Hermes 기능(스킬 등) 산초에도, 채팅에 파일 첨부. 빨리.
- 모델: Fable 5.1 = `claude-fable-5-1` (CLI 실측 3.2초 정상). 노력: `claude --effort` 값 low/medium/high/xhigh/max (2.1.269 실측) → 5단계 슬라이더, 기본 high.
- 첨부: ＋ 버튼·끌어다 놓기·붙여넣기 → `POST /api/upload`(본문 = 파일, x-name 헤더) → data/uploads/ → 지시문 끝에 "[첨부 파일 — Read 도구로 읽어라]" 목록. 이미지·PDF 는 Claude Code 의 Read 가 그대로 본다.
- Hermes 기능은 도구가 아니라 폴더+규칙으로: 스킬 = data/.claude/skills/<이름>/SKILL.md (Claude Code 가 cwd/.claude/skills 를 자동 로드; "스킬로 저장해"·"URL 의 SKILL.md 가져와"), 위키 = data/wiki/*.md, 대화 검색 = history.jsonl Grep, 배달 = 예약 prompt 에 "메일로 보내라" + 연결된 앱 권한(claude.ai 커넥터: Gmail·캘린더·드라이브·M365 가 헤드리스에서도 connected 로 보임, allowedTools 에 `mcp__claude_ai_Gmail` 등). 플레이북 원장은 스킬로 흡수. cmd/net 가드는 Claude Code 권한 체계가 대신.
- 화면: 왼쪽에 스킬·위키 섹션(펼치면 `GET /api/file` 로 읽어 렌더링, 그 두 폴더만 허용), 사이드바는 내용이 바뀔 때만 다시 그려 펼친 항목이 10초마다 접히던 것 해결.
- selftest 추가: 첨부 저장·전달, 위키 목록·읽기·경로 차단.

## 2026-09-13 (토) 새벽 2차 — 미리보기·파일 카드·대화 고정·음성·문서 만들기
- 부장님 요청: 이미지 첨부 미리보기+확대(Claude 앱처럼), 좌측 대화 목록 고정(3개)·최근 3~5개·이동, 엑셀·PPT 만들고 볼 수 있게, 파이스처럼 음성 호출. "전부 Claude 와 같은 기능이라고 생각해."
- 첨부 미리보기: 이미지는 입력창 칩에 썸네일, 채팅 말풍선에도 썸네일, 누르면 라이트박스(Esc/클릭으로 닫힘). 기록(history)에 files 를 저장해 새로 열어도 보인다.
- 만든 파일 알아내기: 채팅 시작 전 데이터 폴더 스냅샷(도구 파일·첨부·일지·위키 제외) → 끝난 뒤 새로 생기거나 바뀐 파일을 `files` 이벤트로 → "파일 N개" 카드(보기·열기·⬇). Write/Edit 도구 경로도 합친다. `GET /api/download`(MIME 로 브라우저가 이미지·PDF·글을 바로 보여줌) · `POST /api/open`(cmd start = 이 PC 기본 앱). 경로는 데이터 폴더 안(허용 시 홈·산초 폴더)만.
- 대화 목록: 고정됨(3개까지, `POST /api/session/pin`) · 최근 5개 + 더 보기 · ✕ 삭제(`/api/session/remove`). 항목에 마우스 올리면 📌 ✕.
- 문서 만들기: Anthropic 의 docx/pptx/xlsx 스킬은 라이선스가 "Anthropic 서비스 이용 약관에 따름"(독점)이라 공개 저장소에 넣지 않음. 대신 산초 기본 스킬 `skills/office-docs/SKILL.md`(openpyxl·python-pptx·python-docx·pdfplumber 요령, 파일함/ 저장)를 만들고 서버가 첫 실행에 data/.claude/skills 로 복사. 이 PC 에 Python 3.14 있어 라이브러리 4개 설치.
- 음성(파이스 방식, 브라우저 내장): 🎤 눌러 말하기(1.2초 침묵 뒤 자동 전송), "산초야" 호출 대기(오인식 보정: 산쵸·삼초·산처·싼초·산조·산쪼·상초), 이름 뒤에 말이 이어지면 바로 전송·이름만 부르면 삐 소리 뒤 듣기, 답 읽어주기(speechSynthesis ko-KR, 마크다운 제거). 음성 턴엔 서버가 "두세 문장으로 짧게" 지시를 덧붙인다. 설정은 브라우저 localStorage.
- selftest 추가: 만든 파일 files 이벤트, 내려받기·열기 경계, 대화 고정·삭제.
- 실전(opus): "엑셀 파일 만들어줘(품목현황, 수식 합계)" → Skill 도구로 office-docs 읽음 → 스크립트 작성·실행·삭제 → 파일함/2026-09-13_품목현황.xlsx(5행, =B2*C2 · =SUM) → files 카드, 내려받기 200(5,371B). 31초. Windows Claude Code 가 PowerShell 도구를 먼저 써서 거부당한 뒤 Bash 로 다시 했음 → allowShell 에 PowerShell 추가.

## 2026-09-13 (토) 새벽 3차 — 다크 모드 · 글자 스트리밍 · 파이스 기능 이식 · 🧠 뇌 그래프
- 부장님: "음성 잘 되네. 다크모드랑 글자 스트리밍, 그리고 파이스에 붙은 모든 기능·능력 다 때려 넣어. 뇌 모양 만든 것도."
- 다크 모드: 색을 전부 CSS 변수로, `<html data-theme>` + prefers-color-scheme. ⚙ 테마 자동/밝게/어둡게.
- 글자 스트리밍: `--include-partial-messages` → stream_event text_delta → `delta` 이벤트. 스트림을 받았으면 통문장(assistant) 은 중복이라 버림. 화면은 프레임마다 마크다운 다시 그림.
- 파이스 이식(도구 코드 0 원칙 유지): 자동 기억(reflect → 프롬프트 규칙 "(기억함)"), 옵시디언 볼트(설정 vaultPath → --add-dir + 규칙), 텔레그램 배달(예약 결과 sendMessage), 비상두뇌(Gemini generateContent, 한도 오류 때만) + 안전모델(본모델 거부 시 Sonnet 재시도), 외부 접속 토큰(x-token/쿼리, SANCHO_HOST), /health, 회의록(연속 받아쓰기 → 첨부 → 정리 요청). 회사 전용(NAS·플랫폼·조직도·계약 메모리) 은 제외.
- 🧠 뇌 그래프: 파이스 public/neural.js(747줄, 힘 기반 그래프 + 오브 + 뇌 실루엣 대기화면)를 그대로 복사, 엔드포인트만 `/api/graph`(산초 → 기억·위키·스킬·대화·예약, 위키 [[링크]])와 `/api/file`, 토큰 헤더 `x-token` 으로 치환. 헤더 LED(CORE ONLINE/THINKING) + 🧠 층. 처음 열 때만 스크립트를 읽는다.
- selftest: 글자 스트림 순서·합, 접속 토큰 401/200, 뇌 그래프 재료, neural.js 제공.

## 2026-09-13 (토) 새벽 4차 — 독립 실행 · Windows 자동 실행
- 부장님이 PowerShell 닫아도 되냐 물으심 → 산초가 이 Claude 세션의 배경 작업으로 떠 있던 것을 발견, 독립 창(cmd 최소화)으로 옮김. 부장님이 그 창을 실수로 닫아 꺼짐 → 다시 켬.
- Windows 시작 시 자동 실행: ⚙ 토글 → `POST /api/autostart` → 시작 프로그램 폴더(APPDATA\…\Startup\Sancho.lnk) 에 바로가기(cmd /c sancho-autostart.bat, 최소화). PowerShell 은 -EncodedCommand 로 따옴표 문제 회피. `sancho-autostart.bat` 은 SANCHO_NO_BROWSER=1 로 브라우저를 안 연다. 실행기 창 제목 "Sancho - closing this window stops Sancho".

- 부장님이 최소화 창을 또 닫아 꺼짐 → "자동으로 실행하고 숨겨줘". 자동 실행 바로가기를 powershell -WindowStyle Hidden → Start-Process cmd … -WindowStyle Hidden(-EncodedCommand) 로 바꿔 창 없이 돌게 함. 지금 인스턴스도 같은 방식으로 숨겨 켬. 끄는 길로 ⚙ "산초 끄기"(`POST /api/quit`, 코드 0 → bat 루프 종료) 추가.

## 2026-09-13 (토) 아침 — 파이스 66개 도구 대조 · 옵시디언
- 부장님: "파이스 기능 전부 다 넣어. 옵시디언도 연결해." 파이스 tools.js 의 도구 66개와 src 모듈 48개를 하나씩 대조해 README 표로 정리. 추가한 것: 감시(schedule repeat "every", 최소 5분, "변화 없음" 이면 일지 생략), 백업(⚙ → data/backups zip, 카드), pc-tools 스킬(클립보드·화면 캡처·알림·앱 열기·시스템 상태·HWPX), 규칙(잊기·프로젝트 노트·Task 위임). 회사·장비 전용은 제외 목록으로 명시.

- 옵시디언: 이 PC 엔 Obsidian 도 볼트도 없었다(obsidian.json 없음). 부장님 볼트는 회사 맥 `~/My-ai`(410MB, md 104,229개). `ssh macw 'tar czf - My-ai' | tar xzf -` 로 `C:\Users\USER\Documents\My-ai` 에 47초 만에 복사(104,227개; 12개 경고 — 애플 xattr 2건 + 20_도면색인 아래 너무 긴 파일명 약 10건). 산초 설정 vaultPath 지정, 볼트 안 `산초/` 폴더 생성, 매일 05:30 맥→PC 한 방향 동기화 예약(vault-sync). 산초가 볼트에 쓴 노트는 맥으로 안 올라간다(한 방향) — 필요하면 다음 판.
- 함정 재확인: Git Bash 의 curl `-d` 에 백슬래시 경로를 넣으면 이 도구가 `\\` 를 `\` 로 접어 JSON 이 깨진다 → 경로는 슬래시(`C:/…`)로.

## 2026-09-13 (토) 오전 — 세종 플랫폼 WBS 이식
- 원본: Sejong-Platform-v2/modules/projects/wbs.html(4,667줄, Firestore·ITP·승인·Rev 이력 포함) + wbs-share.html(읽기 전용 공유 뷰). 산초엔 핵심만: 계층 공정표(lv 0/1/2, 순서 = 계층), 가중 합산 진도율(wCS/childW), 계획 진척(evmsPlanPct: 기간 대비 직선), EVMS(PV·EV·SV·SPI, 대단락 가중), S-곡선(월말 PV), 지연 판정, 일정 건강색(scheduleHealthRGB 이식), 간트(월 달력·오늘선), 접기/펼치기, 스냅샷·복원(Rev 이력 대응), 인쇄.
- 산초 방식: 데이터는 `data/wbs/<프로젝트>.json`(Claude 가 채팅으로 만들고 고침, wbs 스킬이 형식 규정), 계산은 server.js `wbs계산`(단일 진실, selftest 로 숫자 검증), 화면은 `public/wbs.html`(왼쪽 WBS 목록에서 새 탭). ITP·NCR·Firestore·부서장 승인·자동 WO 등록은 회사 전용이라 제외. 플랫폼 원칙 "AI 가 wbsData 직접 조작 금지"는 산초에선 뒤집힘(채팅으로 고치는 게 목적) — 대신 스냅샷 이력으로 되돌린다.

### 한도 관련
- 제 작업은 한도에 걸리지 않고 01:00 무렵 끝났습니다. 사용량 % 는 제가 볼 수 없어 "95% 에서 멈추기" 는 지킬 수 없었고, 대신 단계마다 커밋하고 재개 예약(매시 17분 크론 + 05:35 예약 작업)을 걸어 두었다가 끝나서 지웠습니다.

## 2026-09-14 (일) — 산초 플랫폼: 파이스 + 세종 플랫폼 v2 결합
부장님 정의: **"산초 = PAIS + sejong-platform-v2 결합. 쉬운 사용·매뉴얼·기능. 플랫폼을 만들어 팔 거니 이쁘게. 색은 화이트."**
요구 화면 순서: 대시보드(산초 채팅) · 일정(캘린더) · 프로젝트(WBS) · 메일정리 · 메신저 · 회의실/회의록 · 목표관리 · Manday Tracker · 조직/권한 · 관리 · 결재 · 로드맵·진척 · 지식노트(그래프). 제작공정관리(Traveller)는 제외. 부서 업무도 "아로하처럼" 보이게.

### 먼저 한 일 — 원본 읽기(에이전트 11개 병렬)
세종 플랫폼 v2(index.html 7,968줄 + 모듈 18장 31,562줄 + roadmap.html)를 영역별로 나눠 읽혀 지도를 만들었다(각 영역의 UI 구조·Firestore 컬렉션/필드·함수 줄번호·이식 난이도). 산출물은 scratchpad/map/*.json 11장. 이 지도가 이후 모든 이식의 설계도.

### 기반 3개
1. **저장소 `/api/db`** — 컬렉션 하나 = `data/db/<이름>.json` = `{id: 문서}`. 깊은 병합(점 경로·`__inc`·`__union`), 일괄 쓰기(`_batch`), **SSE 변경 알림**(`_events`) + 폴더 감시 → 두뇌가 파일을 고쳐도 열려 있는 화면이 따라 바뀐다. 서버가 `createdAt/updatedAt` 자동.
2. **`public/m/sdb.js`** — Firestore 함수 이름(collection·doc·getDocs·setDoc·updateDoc·onSnapshot·query·where·writeBatch·serverTimestamp·auth …)을 그대로 흉내 낸 클라이언트. 덕분에 플랫폼 원본 코드가 거의 그대로 돈다. + `window.SANCHO`(ask/open/title/toast/dl/api) — 모듈 ↔ 껍데기 다리.
3. **`public/m/platform.css`** — 세종 플랫폼 CSS 를 화이트 톤 변수로 옮긴 공용 규칙(사이드바·헤더·카드·표·칸반·간트·모달·폼·인쇄).

### 껍데기(public/index.html) 재작성
왼쪽 사이드바(메뉴 14 + 부서 도구함 트리 — 부서·도구 구성은 `depts` 컬렉션이라 조직 화면에서 바꿀 수 있다), 상단 헤더(전역 검색 `/api/search` · 알림 벨 · ⚙ 설정 · 주인 칩), 본문은 iframe 모듈. **대시보드 자리에 산초 채팅** — 인사말(이름·직급·회사·날짜) + 오늘 KPI 4장(일정·결재·긴급 메일·진행 프로젝트, 누르면 그 화면) + 빠른 지시 버튼 6개, 그 아래 기존 채팅(스트리밍·음성·첨부·파일 카드·뇌 그래프·회의록 녹취 전부 유지). 아직 안 만든 메뉴는 "준비중" 배지 + 안내 화면(404 대신). `/api/state` 가 만들어진 화면 목록(modules·tools)을 주므로 파일만 놓으면 메뉴가 켜진다.

### 화면 (public/m/*.html)
일정 · 프로젝트(카드·연간 통합 간트·업무 칸반·WBS 연결) · 메일정리 · 메신저(산초도 대화 상대) · 회의실/회의록(녹취 → 산초가 정리) · 목표관리(OKR) · 조직/권한 · 관리 · 결재 · Manday · 로드맵 · 지식노트 · **도움말(매뉴얼)**. 모든 화면에 `✳ 산초에게 시키기` — 그 화면에 맞는 완전한 지시문이 채팅창에 채워진다.
두뇌용 규격은 `skills/platform/SKILL.md`(컬렉션·필드 표 + "이렇게 시키면 이 파일을 고쳐라").

### 검사 도구 probe.js (새로 만듦)
크롬을 숨겨 띄워 **콘솔 오류 + 실제 DOM + 시킬 JS(버튼 클릭·모달 확인)** 를 받아온다. 교훈: 크롬 `--dump-dom` 은 SSE(EventSource) 를 여는 화면에서 **영영 끝나지 않는다** → CDP(WebSocket, Node 24 내장)로 붙어서 받아야 한다. 시험 데이터는 scratchpad/seed.mjs(14개 컬렉션 52문서). 이식은 계약서(SPEC.md · SPEC-TOOLS.md)를 에이전트에게 먼저 읽히고, probe 검사를 통과해야 완료로 친다.

### 한도 사고
작업 중 Fable 5.1 월 한도에 걸려 모듈 제작 에이전트 6개가 동시에 죽었다(파일은 이미 써 둔 상태여서 살렸다). 부장님이 Opus 5 로 바꿔 재개. 실서버 산초 모델도 fable → opus 로 바꿔 두었다. 그 뒤 Opus 세션에서도 월간 지출 한도(429)에 걸려 에이전트 작업이 중단되었다.

## 2026-09-14 (일) 낮 — 부서 도구함 정리 · 플랫폼 범용화 · 원격 작업 로드맵 수립 (Antigravity 세션)
- 부장님 결정: "부서 도구함은 없어도 돼. 다른 사람들이 쓸 수 있게 만드는 게 목표니 사내 정보가 나가지 않게 없애고, 깃허브에서 다른 곳에서도 작업할 수 있게 로드맵 만들어 올려줘."
- **부서 도구함 완전 제거**:
  - `public/m/tools/` 폴더 및 회사 고유 계산식/장비/품질 도구 삭제
  - `public/index.html` 사이드바에서 부서 도구함 트리 및 TOOLS 라우팅 제거 (깔끔한 13개 메뉴 사이드바로 정제)
  - `public/m/org.html` 부서 모달에서 도구함 체크박스 제거 → 범용 부서 정보 관리로 단순화
  - `public/m/help.html` 및 `skills/platform/SKILL.md` 도구함 설명 정제
- **서버 및 프론트 버그 수정**:
  - `server.js` 514행의 구버전 중복 `wbs` 키 제거
  - `public/index.html` refresh 내 `있는도구` ReferenceError 예외 수정 (probe.js 로 발견 및 즉시 교정)
- **원격 협업 로드맵 수립**:
  - `ROADMAP.md` 신규 작성: Zero-dependency 설계, 13개 모듈 현황, 타 PC 클론 및 `sancho.bat`/`node server.js` 구동 가이드, `selftest`/`probe` 검증법, 향후 마일스톤 단계 정리
- **자가 검증**:
  - `node selftest.js` 75개 전체 통과
  - `probe.js` 메인 화면 및 각 모듈 화면 DOM 렌더링 정상, 콘솔 에러 0건 확인



## 2026-09-14 (일) 오후 — 안티그래비티 작업 점검 + 워크플로(n8n 식 자동화)
부장님이 제 한도가 걸린 사이 구글 안티그래비티로 작업(멀티유저 로그인·로그인 화면·콕핏 UI). "코드상 문제 없는지 전부 확인해 리뷰하고, n8n 처럼 워크플로 짜는 것(MS Copilot 비슷)도 넣어서 마무리해라."

### 점검에서 나온 것 — 고친 순서대로
1. **네트워크 전면 개방(치명)**: `listen(..., '0.0.0.0')` 로 바뀌어 있어 같은 망의 누구나 접속 가능했다. 산초는 이 PC 의 Claude Code 로 명령을 실행할 수 있으니(allowShell·allowHome 켜짐) 사실상 원격 조작 구멍. → 기본 127.0.0.1 복귀, 밖으로 열 때만 경고 출력.
2. **기본 계정 admin/admin123 자동 생성(치명)**: 로그인 화면에도 적혀 있었다. → 자동 생성 제거, 계정이 없으면 "관리자 계정 만들기"(8자 이상), 이미 심어진 admin 계정 삭제.
3. **인증 우회 환경변수**: `SANCHO_SKIP_SELFTEST`(재시작 관문이 쓰는 값)가 켜지면 로그인을 통째로 건너뛰었다. → 자가시험 전용 `SANCHO_TEST_USER` 로 분리.
4. **세션이 메모리에만**: 자기 수정·업데이트로 재시작할 때마다 전원 로그아웃. → `data/sessions.json`(30일).
5. **부장님 데이터 실종**: 로그인이 붙으면서 개인 파일이 `data/users/<계정>/` 로 분기됐는데 옛 파일(설정·기억·예약·대화 72KB)은 `data/` 에 남아 화면에서 사라진 것처럼 보였다. → 첫 로그인 때 한 번 이사(`이사()`), 실서버에서 예약·기억·대화 복구 확인.
6. **예약이 영영 안 돎**: 시계(tick)는 세션 밖에서 도니 사용자 폴더의 schedule.json 을 못 봤다. → 사용자별로 컨텍스트를 씌워 실행, 일지도 사용자별.
7. 화면도 로그인 문지기 뒤로(로그인 전 `/login.html` 로), `/api/me` 복구, 쿠키 파싱 `=` 처리, 비밀번호 변경 API, 임시 패치 스크립트(update_*.cjs·temp*.html) 저장소에서 제거.
자가시험에 로그인 검사(기본 비번 거부·짧은 비번 거부·setup 재실행 차단·HttpOnly)를 넣어 관문이 다시 막도록 했다.

### 워크플로 (새 기능)
- 화면 `public/m/workflow.html`: 캔버스에 노드를 놓고 포트를 끌어 선으로 잇는다(n8n 식). 왼쪽 목록, 오른쪽 설정, 아래 실행 기록(단계별 ✅/❌/⏳·결과 미리보기·과거 실행 선택).
- 엔진은 `server.js` 안(의존 0): 트리거 노드부터 선을 따라가며 실행, 조건 노드는 참/거짓 포트로 분기, 단계마다 `db/workflowruns` 에 기록 → 화면이 SSE 로 실시간 표시. 최대 50단계, 실패 시 중단(노드별 "실패해도 계속" 옵션).
- 노드 12종: start·schedule·watch·ai(산초에게 시키기)·dbRead·dbWrite·if·http·telegram·message·journal·wait·set. 템플릿 `{{steps.이름}}`·`{{today}}`·`{{trigger.input}}`.
- 시간 트리거는 예약과 같은 시계에 얹어 사용자별로 실행. 산초가 말로 만들 수 있게 platform 스킬에 형식을 적고, 실서버에 샘플 2개(아침 브리핑·메일 정리 → 급한 것만 알림)를 자동 실행 꺼진 상태로 넣어 두었다.
- 자가시험에 워크플로 검사(분기·템플릿 치환·기록·없는 흐름 오류) 추가.

### 점검 결과
화면 15장(껍데기 + 모듈 14) 전부 probe.js 실행 검사 통과 — 콘솔 오류 0, 실패 응답 0. 자가시험 통과(화면 문법 16장 포함). 남은 것은 부서 도구함 11개 도구(ITP·QA Doc·NCR·CAR·품질종합분석표·모바일 검사·측정기구·자산·교반기·압력용기·구매일지) — 한도로 에이전트가 모두 죽어 아직 "Soon" 이다.

## 2026-09-15 (월) — "화면이 또 안 된다" 의 진범 두 가지
부장님: "사이드 도구함들이 왜 또 안되지?? 일정 메일 프로젝트 등등 사용 안 되네."

1. **sancho.bat 자동 복구가 새 코드를 덮어썼다(두 번째 사고).** 내가 실서버를 Stop-Process 로 내릴 때마다 종료 코드가 0·75 가 아니어서 bat 이 크래시로 판단하고 `git checkout last-good -- .` 를 실행했다. 그래서 커밋까지 끝낸 새 server.js·index.html 이 옛 판으로 덮여, 고쳐 놓은 것이 화면에서는 그대로 안 되는 상태였다(9/14 "자동 복구 스크립트로 로그인 시스템 원복" 과 같은 원인). → 이제 크래시로 멈춰도 `node --check server.js` 가 통과하면 파일을 건드리지 않고 그대로 다시 켠다. 정말 깨졌을 때만 `git stash` 로 사본을 남기고 되돌린다. 서버를 내릴 땐 ⚙ "산초 끄기"(`POST /api/quit`).
2. **로그인 락아웃.** 계정을 만든 뒤로는 쿠키 없는 브라우저에서 모든 화면이 302 로 로그인 페이지로 튕기는데, 그 튕김이 iframe 안에서 일어나 본문만 비어 보였다(= 메뉴가 안 먹는 것처럼). 게다가 껍데기는 401 을 받으면 옛 "접속 토큰" 프롬프트로 빠졌다. → ① 이 컴퓨터(127.0.0.1)에서 직접 열었고 밖으로 열어 두지 않았으면 로그인 없이 쓴다(⚙ 스위치, 공용 settings.json 에 저장, SANCHO_HOST 로 열면 무조건 로그인 — 자가시험으로 두 경우 다 확인) ② iframe 안에서 로그인 페이지가 열리면 창 전체를 로그인으로 ③ 401 이면 로그인 상태를 확인해 로그인 화면으로 보내고 끝나면 원래 화면(?next=)으로 돌아온다.

확인: 실서버에서 쿠키 없이 일정·메일·프로젝트·워크플로 네 화면을 실제 브라우저로 눌러 정상 렌더 확인(콘솔 오류 0).

## 2026-09-15 (월) 오전 — 사이드바 7번째 메뉴(목표관리~도움말) 먹통 원인 규명 및 해결 (Antigravity)
부장님: "저기 붉은색으로 되어 있는곳에 .. 매뉴들이 작동 안해.. 이거 왜 자꾸 이러지.. 어제부터 몇번 고쳤는데.. 사이드바 매뉴들 전부 작동 잘되게 만들어놔봐"

### 원인 규명 (크롬 HTTP/1.1 호스트당 6개 연결 한도 고갈)
1. **증상**: 대시보드(0), 일정(1), 프로젝트(2), 메일정리(3), 메신저(4), 회의실(5)까지는 정상 작동하나, 7번째 메뉴인 **목표관리(OKR)**부터 아래 모든 메뉴(공수, 조직/권한, 관리, 결재, 로드맵, 지식노트, 워크플로, 도움말)를 누르면 화면이 하얗게 멈추고 좌하단에 `🔴 서버 응답 없음` 표시.
2. **메커니즘 분석**:
   - `public/index.html` 은 각 메뉴를 전환할 때 `<iframe class="frame">` 을 생성하고 DOM 에 유지(캐시)함.
   - 각 iframe 은 `/m/sdb.js` 를 불러와 `onSnapshot()` 을 호출하며, 이때 `new EventSource('/api/db/_events')` 를 각각 개설함.
   - `index.html` 본체(1) + 앞선 5개 iframe(5) = **정확히 6개의 지속적 SSE 연결(keep-alive)이 유지됨**.
   - 크롬/엣지 등 모든 브라우저는 동일 호스트(`127.0.0.1`)에 대해 **동시 HTTP/1.1 TCP 연결을 최대 6개로 하드코딩 제한**함.
   - 7번째 메뉴(OKR)를 누르는 순간 브라우저는 7번째 연결(`GET /m/okr.html` 및 `/api/auth/me`)을 큐에 넣고 **영구 대기(Stalled)** 상태로 빠짐.
   - 연결 슬롯이 없어 10초마다 도는 `refresh()` (`/api/state`)마저 타임아웃 → `🔴 서버 응답 없음` 발생.

### 해결책
1. **단일 SSE 허브 도입 (`index.html` & `sdb.js`)**:
   - 부모 창(`index.html`)이 단 1개의 SSE 연결을 유지하고, 전역 이벤트 구독 버스(`window.__sancho_sub_events`)를 개설.
   - iframe 들(`sdb.js`)은 `window.parent.__sancho_sub_events` 가 있으면 자체 `EventSource` 를 열지 않고 부모의 이벤트를 구독.
   - 단독 실행 시에만 폴백으로 자체 SSE 개설.
   - 이로써 15개 메뉴 전체를 띄워도 **브라우저 연결 소비는 단 1개**로 제한되어 나머지 5개 연결이 즉시 API/페이지 요청을 처리.
2. **iframe 부팅 최적화 (`sdb.js`)**:
   - `window.fb.me()` 호출 시 부모 창에 이미 존재하는 `window.parent.me` 를 즉시 참조하여 불필요한 네트워크 왕복 없이 0ms 로 즉시 `boot()` 실행.
3. **서버 SSE 소켓 정리 강화 (`server.js`)**:
   - `/api/db/_events` 에서 `req.on('close')` 외에도 `res.on('close')`, `res.on('error')`, `req.on('error')` 핸들러를 붙여 연결 해제 즉시 구독자 풀에서 정리.
4. **부수 결함 교정**:
   - `index.html` 내 미정의 참조(`현재도구`, `openTool`) 제거 및 `window.onerror` 안전화.
   - `hashchange` 이벤트 리스너 추가로 브라우저 뒤로가기/앞으로가기 및 해시 점프 정상 연동.

### 검증 결과
- `node selftest.js` 75개 전원 통과.
- `probe.js` 로 15개 메뉴 전체(`dashboard` ~ `help`) 연속 전환 테스트: 15개 메뉴 전원 **STATUS: OK**, DOM 정상 렌더링, 콘솔 에러 0건.

